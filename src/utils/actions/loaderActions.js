const openLoaderAction = () => ({ type: 'OPEN_LOADER' });

const closeLoaderAction = () => ({ type: 'CLOSE_LOADER' });

const loaderActions = {
  openLoaderAction,
  closeLoaderAction,
};

export default loaderActions;
